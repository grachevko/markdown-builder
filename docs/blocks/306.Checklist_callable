Markdown::builder()->checklist(static function (ChecklistBuilder $builder): void {
        $builder
            ->addLine('Hallo', false)
            ->addLine('foo', false)
            ->addLine('bar', true);
    })->getMarkdown();
